// criar um programa para verificar se a nota for acima de 60 'aprovado' e se não 'reprovado' usando operador terminário
let nota = 59
let resultado = nota >= 60 ? 'passou de ano':'não passou'
console.log(resultado)